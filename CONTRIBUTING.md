# Contributing

This repository is **internal-only** and does not accept external contributions.

Changes are made by the Agnostic Security team via reviewed pull requests: every change to the default branch requires review and approval (see `.github/CODEOWNERS`). Pull requests opened from forks will not be actioned.
